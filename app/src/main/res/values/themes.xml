
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Kalma" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/md_theme_primary</item>
    </style>

    <!-- Splash theme with pastel background -->
    <style name="Theme.Kalma.Splash" parent="Theme.Kalma">
        <item name="android:windowBackground">@color/splash_bg</item>
    </style>
</resources>
